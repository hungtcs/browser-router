import './router';
import './router-link.element';

async function main() {

  // const routerLinkElements = document.querySelectorAll('a[router-link]');

  // routerLinkElements.forEach(routerLinkElement => {
  //   routerLinkElement.addEventListener('click', () => {
  //     const link = routerLinkElement.getAttribute('router-link')!;
  //     router.navigate(link);
  //   });
  // });

}

main().catch(err => console.error(err));
